<nav class="navbar navbar-dark bg-dark navbar-expand-md fixed-top" id="navbar">
    <div class="container" id="div"><a class="navbar-brand" id="brand" href="<%= root_url %>">&emsp;Trips&emsp;</a><button class="navbar-toggler" data-toggle="collapse" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse"
            id="navcol-1">

              <ul class="nav navbar-nav ml-auto">
                  <li class="nav-item-Nico" role="presentation"><%= link_to "Trips", trips_url, class: "nav-link" %></li>
                  <% if user_signed_in? %>
                  <li class="nav-item-Nico" role="presentation"><%= link_to "Locations", locations_url, class: "nav-link" %></li>
                  <% end %>
              </ul>

              <% if user_signed_in? %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-success"  %>
              <% else %>
              <%= link_to "Sign In", new_user_session_url, class: "btn btn-success"  %>&nbsp;
              <% end %>
        </div>
    </div>
</nav>
